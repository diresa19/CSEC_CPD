n,h= map(int,input().split())
total_width = 0
heights= list(map( int, input().split()))
for height in heights:
    if height <= h:
        total_width += 1
    else:
        total_width += 2
print(total_width)
