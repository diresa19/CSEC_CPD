matrix = []
for i in range(5):
    row = list(map(int, input().split()))
    matrix.append(row)

for r in range(5):
    for c in range(5):
        if matrix[r][c] == 1:
          
            moves = abs(r - 2) + abs(c - 2)  
            print(moves)
            break
